# Dependencies for local_gpu_serve.py — loads LLM into GPU VRAM, runs LoRA training.
# Used with Dockerfile.model.gpu for local dev on NVIDIA GPU machines.
# NOT needed in cloud deployment — see requirements.model.txt for hf_serve.py.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
requests==2.32.3
python-dotenv==1.0.1
boto3

# ML / GPU stack
torch==2.4.1
transformers==4.45.2
peft==0.13.0
trl==0.11.4
accelerate==1.0.1
bitsandbytes==0.44.1
sentencepiece==0.2.0
datasets==3.0.1
