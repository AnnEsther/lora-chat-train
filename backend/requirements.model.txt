# Dependencies for hf_serve.py — the HF vLLM inference proxy.
# No ML frameworks, no GPU libraries.
# For local GPU training deps see Dockerfile.model.gpu / requirements.model.gpu.txt

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.2
python-dotenv==1.0.1
